// Article.cpp
#include "Article.h"

Article::Article(std::string title, std::string author, int tags, int readCount)
    : title(title), author(author), tags(tags), readCount(readCount)
{
    // 构造函数体，如果有需要初始化的操作，可以在这里添加
}
